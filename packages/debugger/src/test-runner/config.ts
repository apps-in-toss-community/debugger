/**
 * Placeholder for the test-runner's public Node-side API
 * (`definePhoneTestConfig` / `definePhoneVitestConfig` in the real
 * implementation). Vendored from `devtools`'s `src/test-runner/config.ts`
 * (frozen at devtools#813, "SPLIT FREEZE") in D2.
 */
export const TEST_RUNNER_PLACEHOLDER = '@ait-co/debugger/test-runner' as const;
